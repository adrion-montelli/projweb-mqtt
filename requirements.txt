# Django Framework
Django==4.2.7
django-extensions==3.2.3

# Web Server
gunicorn==22.0.0

# Database
# Usar PyMySQL (pure-Python) em Windows para evitar necessidade de headers nativos
PyMySQL==1.1.1

# Environment & Configuration
python-decouple==3.8
python-dotenv==1.0.0

# API & Data Processing
djangorestframework==3.14.0
django-cors-headers==4.3.0

# Frontend & Assets
# A versão 4.1.3 não está disponível no PyPI para o ambiente atual; usar uma versão compatível
django-compressor==4.6.0

# Security & Auth
# Note: 'django-defense' not available on PyPI for this environment; remove or replace if needed

# Utilities
requests==2.31.0
pytz==2023.3

